npm install -g @angular/cli

npm install -g yarn

ng set --global packageManager=yarn

	in frontent folder:

yarn install

	start server(localhost:4200):

yarn run start

	If backend server is different from localhost:2380
	
	make appropriate change in frontend/proxy.conf.json